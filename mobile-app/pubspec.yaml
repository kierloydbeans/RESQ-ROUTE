name: resq_route_mobile
description: RESQ-Route Mobile App for Citizens and Rescuers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.31.0
  geolocator: ^10.1.0
  web_socket_channel: ^2.4.0
  http: ^1.1.0
  provider: ^6.1.0
  qr_flutter: ^4.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
